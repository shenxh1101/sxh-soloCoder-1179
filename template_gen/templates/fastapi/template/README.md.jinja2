# {{ project_name }}

A FastAPI application{% if use_docker %} with Docker support{% endif %}{% if use_database %} and database integration{% endif %}.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --reload
```

{% if use_docker %}
## Docker

```bash
docker-compose up -d
```
{% endif %}

## Author

{{ author }}