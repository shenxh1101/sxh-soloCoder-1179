version: "3.8"

services:
  app:
    build: .
    ports:
      - "{{ docker_port }}:{{ docker_port }}"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port {{ docker_port }} --reload